#include<iostream>
using namespace std;

void sort(int arr[], int size)
{
    
}

int main()
{
    int arr[] = {12, 23, 34, 45, 56};
    int size = sizeof(arr) / sizeof(arr[0]);
    sort(arr, size);
    return 0;
}